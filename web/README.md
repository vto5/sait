# Web module

Frontend-модуль на React + Vite для сайта услуг промышленной безопасности.

## Запуск

```bash
npm install
npm run dev
```

## API для заявок

По умолчанию заявки отправляются в `http://localhost:8000/api/leads`.
Настройка через переменную `VITE_API_BASE_URL`.
